# Dependencias del deploy (Render Free — modo RAG estático, SIN ChromaDB).
# El entrenamiento es solo local: pip install chromadb && python setup/train_vanna.py
vanna>=0.1.0
google-genai>=1.0.0
plotly>=5.0.0
dash>=2.0.0
pandas>=2.0.0
python-dotenv>=1.0.0
gunicorn>=20.0.0
